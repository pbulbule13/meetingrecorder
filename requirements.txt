# Minimal Working Requirements for Nexus Assistant
# Python 3.11+
# These packages work without C++ compiler

# Core Framework (required)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
httpx==0.28.1

# Logging (required)
loguru==0.7.3

# Data Processing (required)
numpy>=1.24.0

# Optional: Vector Database (requires C++ Build Tools on Windows)
# Install separately if needed: pip install chromadb sentence-transformers
# chromadb==0.5.23
# sentence-transformers==3.3.1

# Optional: LLM Providers (install as needed)
# pip install google-generativeai
# pip install anthropic
# pip install openai

# Optional: Speech-to-Text Providers (install as needed)
# pip install deepgram-sdk
# pip install assemblyai
